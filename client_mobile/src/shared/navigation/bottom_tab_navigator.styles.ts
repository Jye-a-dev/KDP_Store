import { StyleSheet } from 'react-native';

export const tabStyles = StyleSheet.create({
  tabBar: {
    backgroundColor: '#fff',
    borderTopWidth: 2.5,
    borderTopColor: '#111',
    height: 64,
    paddingBottom: 8,
    paddingTop: 6,
  },
  tabLabel: {
    fontSize: 10,
    fontWeight: '900',
    letterSpacing: 0.5,
  },
});
